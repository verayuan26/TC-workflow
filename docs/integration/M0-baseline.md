# M0 基线、保护与缺项记录

日期：2026-09-12（Asia/Shanghai）

## 已验证

- 目标仓库仍为 `verayuan26/TC-workflow`，远端 `main` 为 `356f6bb059889ca9314ea54cee098364dbea3b02`。
- 使用独立 worktree 与 `codex/tiger-workbench-integration-v1` 分支，未覆盖原工作目录。
- 更新包和 `inputs/` 内文件 SHA256 全部通过。
- 内容增量可干净应用；9 个结果文件与内容源码 d940fe0 一致。
- 外联源码 ebcd960 的状态模型、推进门槛、CAS、人工验收和 Codex 边界已读取。
- 现有内容生产继续使用本机 `ai-media-tools`、原 SQLite 和 NAS；本工程只持有云端投影、命令和回执。
- 原生产库已有预接入备份及恢复副本；本轮不删除旧库或旧页面。

## 已连接但仍需联测

- 现有内容 API：本机/局域网 8787 服务。
- Voice Box：固定服务身份已绑定 `Vera Russian Clone v1`；首条正式新音轨仍需成功生成并进入剪辑包后验收。
- FFmpeg、素材元数据与 NAS 已存在；统一工作台适配器仍需建立命令回执。

## 未验证 / 外部依赖

- Cloudflare Tunnel、Access 应用、Audience、D1 生产绑定和员工邀请是否已创建；文件固定使用 NAS，不启用 R2。
- GitHub 远端推送/PR、Cloudflare 发布、域名切换、真实外发与付费是否在本次授权范围。
- 指定剪辑师从外网完成一次真实领取、下载、回传成片及工程的结果。

这些缺项只阻塞相应外部步骤，不阻塞本地代码、迁移、测试和恢复演练。

## 数据边界

- 公共仓库仅保存脱敏 schema、示例和导入工具。
- 私有员工映射使用稳定 `person_id`；工作邮箱、社媒登录邮箱、平台用户名分别存储，不互相替代。
- 浏览器不保存权威角色或生产任务；角色来自 Access JWT 与服务端成员表。
