# Tiger Outreach O0—O4 部署验收记录

- 日期：2026-09-14（Asia/Shanghai）
- 源码：`TC-workflow-integration` / `codex/tiger-workbench-integration-v1`
- 部署代码提交：`cb1f6c0`
- Worker：`tiger-unified-workbench` / `f38f0dfe-2f2d-4e80-a54b-533e2f6df6f8`
- D1：`tiger-unified-workbench` / `f6bf2bbf-68d7-49c7-b5ec-76fc7e85e548`
- 范围：复用统一工作台、真实 D1、现有 Access 与 Multica；没有自动联系客户，没有新增付费存储或第二套 CRM。

## 阶段结果

| 阶段 | 状态 | 证据与下一步 |
|---|---|---|
| O0 现状与恢复点 | PASS | 基线 `59858b1`；确认 8787 内容源、Tunnel、Access、D1 与三人范围。部署前备份位于 `/Users/mac/Library/Application Support/TigerOutreach/backups/predeploy-20260914/`。 |
| O1 模块与真实交接 | PASS | 统一客户/来源/批次/任务/事件/附件/检查模型已接入；虚拟 A/B 隔离、服务端拒绝、提交幂等、CAS、附件 SHA、pass/needs_more、下一步及重启恢复通过。 |
| O2 受控上线 | PASS（技术） | 生产 Worker、D1 0004/0005、8788 附件服务和 Tunnel 规则运行；公网入口受 Access 保护，服务域仅开放 Outreach API。已有 Chrome 授权会话加载到 `/outreach` 的生产 HTML；自动化页面内容读取因浏览器控制超时未作为业务员验收证据。 |
| O3 首批导入与准备 | PASS | Multica TIG-371 正式导入 8/8，重复投递命中原批次；生产 D1 独立核对 8 客户、8 来源、8 首联任务、8 批次条目。按老板授权统一分配给 ALAN，客户和任务均为 8/8，仍未联系。 |
| O4 业务员启用与交付 | PARTIAL | 最终花名册为 ALAN（业务主管），阿旺、瑛瑞、Henry、张创治（业务员）；5 人均已写入真实成员表并获 `overview:read/outreach:read/outreach:write`，8 家客户和 8 项任务仍统一归 ALAN。当前 Access 仍仅老板、阿旺、张永琪；Wrangler OAuth 不含 Access 策略写权限，浏览器控制提交又持续超时，因此新增白名单尚未保存。 |

## T01—T14

| 编号 | 状态 | 实际证据 |
|---|---|---|
| T01 | PASS | typecheck、lint、10 个测试、14 项内容不变量及 Vite build 通过；远端迁移 0004/0005 已应用。 |
| T02 | PASS（技术） | `/outreach` 公网命中 Access；部署触发器、静态资源和 `/api/outreach` 路由存在；服务身份生产 API 返回 8 leads / 8 tasks，未被 HTML 兜底。 |
| T03 | PASS（回归基线） | 公网 `/workbench/` 继续由 Access 保护；原 8787 `/workbench/` 返回 200，现有内容服务未切换或停止。 |
| T04 | PASS | 未登录 `/outreach`、`/workbench/`、`/legacy`、附件均 302；无服务令牌 API 为 401；伪造邮箱头仍为 302；服务域非 Outreach 页面为 404；8788 仅监听 loopback。 |
| T05 | PASS（合成身份） | Sales A/B 各只返回本人 1 客户/1 任务；A 写 B 任务返回 403，未扩大生产 Access。 |
| T06 | PASS | 仅 boss 可分配，owner 必须是 active sales 且有 `outreach:write`；跨 owner 开始/提交/附件被服务端拒绝。生产批次已按老板授权分配给 active sales `person_alan`。 |
| T07 | PASS（生产） | TIG-371 首次回执 created=8；同包第二次 `idempotent=true`；D1 仍为 8 客户、8 任务。 |
| T08 | PASS（合成环境） | 真实字节写入 8788，SHA-256 与声明一致后才 verified；随后生成提交事件与服务器回执；重启后仍可见。 |
| T09 | PASS（合成环境） | 重复提交返回同一 event_id；旧 revision 返回 409；同一 active task / follow-up 有唯一约束。 |
| T10 | PASS（合成环境） | 合格事件进入 reviewed 并创建下一任务；缺附件进入 needs_more，补交沿用同一客户和任务；重启后队列可恢复。 |
| T11 | PASS（规则测试） | unreachable/bounced/message_sent、contact_found、willing_to_share、no_current_need、not_fit/do_not_contact 映射到独立进展；`is_test=1` 不进入生产统计。 |
| T12 | PASS（技术） | 生产统计为总数 8、ALAN 所有 8、pending 8、已尝试 0；客户/任务 owner mismatch=0，未知保持 unknown；`/legacy` 只有只读页面，没有写路由。 |
| T13 | PASS | Worker 版本、LaunchAgent PID、Tunnel ingress 均可核对；部署前 D1/config 备份和旧内容源保留，未做破坏性生产回滚。 |
| T14 | PARTIAL | 老板已明确授权 8 家统一分配给 ALAN，并确认 ALAN 为业务主管，阿旺、瑛瑞、Henry、张创治为业务员；真实花名册、成员与任务分配已完成。Cloudflare Access 尚未新增 ALAN、瑛瑞、Henry、张创治（阿旺原已放行），故不能写成“业务员本人已能使用”。 |

## Multica 与批次回执

- TIG-370：真实 Multica 合成交接。dry-run → 测试导入 → 重复投递；1 条 `is_test=1`，三份服务器 JSON 回执已回写，重复投递复用同一 lead/task，不计业务指标。
- TIG-371：本聊天把附件交给现有“业务开发总管”执行正式导入。服务器回执：`total=8, created=8, matched=0, needs_more=0, conflict=0, not_fit=0, failed=0`；重复投递 `idempotent=true`。Codex 读取生产 D1 后回写独立验收并关闭任务。
- TIG-371 分配补充：老板随后明确要求 8 家统一分配给 ALAN；生产 D1 与线上服务 API 均返回 `alan_leads=8`、`alan_tasks=8`、`pending=8`、`uncontacted=8`、`owner_mismatch=0`，分配回执已追加到原 Multica 任务。
- 部分失败：合成批次实际返回 `matched=1 + needs_more=1`、状态 `partial`；合格旧客户追加来源、复用原任务，缺联系方式条目给出具体补项；重复投递仍为同一 2 个批次条目。

## 8 家生产真值

| item_ref | 企业 | 城市 | 导入/任务/归属 |
|---|---|---|---|
| RU8-001 | РМ-Сервис | 叶卡捷琳堡 | created / 首联任务 pending / ALAN |
| RU8-002 | ТЕХНО-ГМ | 车里雅宾斯克 | created / 首联任务 pending / ALAN |
| RU8-003 | ТензоТехСервис | 喀山 | created / 首联任务 pending / ALAN |
| RU8-004 | СтанПро | 纳别列日内切尔尼 | created / 首联任务 pending / ALAN |
| RU8-005 | НПО АЛЛЕС | 下诺夫哥罗德 | created / 首联任务 pending / ALAN |
| RU8-006 | Лигна-С | 萨马拉 | created / 首联任务 pending / ALAN |
| RU8-007 | СнабЭкспрессЦентр | 莫斯科州 | created / 首联任务 pending / ALAN |
| RU8-008 | Ф и Ф | 圣彼得堡 | created / 首联任务 pending / ALAN |

每家都保留来源 URL、2026-09-12 核查日期、证据限制、俄语草稿和中文解释；实际需求、是否接受中国供货和实际联系结果均未由导入推断。

## 运行与恢复

- 主路径：`/outreach*`、`/overview*`、`/settings*`、`/legacy*`、对应 API/资源由 Worker；`/workbench/` 和原内容接口继续到 8787；`/outreach-files/*` 到 8788。
- 服务入口：`outreach-ingest.tigersourcingchina.com` 仅允许受控 Outreach API；令牌只在 Keychain/Worker secret，报告不记录密钥。
- 回退：Worker 可回到部署前版本；Tunnel 配置可从备份恢复；D1 为兼容新增表，不删除原数据；附件服务可单独停止而不影响原剪辑工作台。
- 下一动作：使用具备 Cloudflare `Access: Apps and Policies Write` 权限的临时 API Token，把 ALAN、瑛瑞、Henry、张创治加入现有 `Allow Tiger pilot testers` 策略（阿旺已在策略中）；随后逐人验证登录、只见本人任务，并完成一轮内部演练提交与 Codex 检查。临时 Token 不写入仓库。
